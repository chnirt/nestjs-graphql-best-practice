input CreateRoomInput {
	title: String!
	userIds: [ID!]!
}

type Room {
	_id: ID!
	title: String!
	users: [User!]!
	messages: [Message]
	createdAt: Float!
	updatedAt: Float!
}

type Query {
	rooms: [Room]
	room(_id: ID!): Room
}

type Mutation {
	createRoom(input: CreateRoomInput!): Room
}
