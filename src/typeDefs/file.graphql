type File {
	_id: ID!
	filename: String!
	path: String!
	createdAt: Float!
	updatedAt: Float!
}

type Mutation {
	uploadFile(file: Upload!): Boolean @isAuthenticated
}

type Query {
	files: [File] @isAuthenticated
}
